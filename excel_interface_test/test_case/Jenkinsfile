pipeline{
    agent any  //创建节点
    stages {  //阶段
        stage('build flask') { //子阶段
            steps { //步骤
                bat 'python excel_interface_test/test.py'
    // One or more steps need to be included within the steps block.
  }
}
}
}
